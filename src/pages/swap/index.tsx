import React from "react";

import { Layout } from "src/components/common";

import styles from "./index.module.scss";

const Swap: React.FC = () => {
  return <div>This is Swap Page</div>;
};

export default Swap;
