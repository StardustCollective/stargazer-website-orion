import React from "react";

import { Layout } from "@components/common";

import styles from "./index.module.scss";

const Swap: React.FC = () => {
  return <Layout>This is Swap Page</Layout>;
};

export default Swap;
