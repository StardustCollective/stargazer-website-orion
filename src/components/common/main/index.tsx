import React from "react";
import styles from "./index.module.scss";
import { Button } from "@components/common";

export const Main: React.FC = () => {
  return <div className={styles.main}>This is main page</div>;
};
